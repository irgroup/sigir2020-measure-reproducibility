The scripts should be run in the following order:
1) rename_runs: to rename the runs and save them in the same directory
2) reformat_runs: to change the topic id inside the run file and remove extra documents/topics